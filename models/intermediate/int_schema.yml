version: 2

models:
  - name: int_order_reviews
    description: "Deduplicated order reviews — keeps most recent review per order"
    columns:
      - name: review_id
        description: "Unique review identifier after deduplication"
        tests:
          - not_null
      - name: order_id
        tests:
          - not_null